[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prediction-arb-scanner"
version = "0.1.0"
description = "Scanner-first prediction markets arbitrage research engine."
requires-python = ">=3.11"

